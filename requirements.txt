# Tested on Python 3.12.10. All pinned versions below are compatible
# with Python 3.12.x (no changes needed for 3.12.10 specifically).

# --- Core PDF parsing ---
pymupdf==1.24.10
# --- Word document parsing (content ingestion + narrative script templates) ---
python-docx==1.1.2
# --- OCR dependencies (Docker-based — no host Tesseract binary needed) ---
# 'docker' is the Python SDK for talking to the local Docker daemon.
# 'Pillow' is used to convert PyMuPDF pixmaps to PNG before sending to Docker.
docker>=7.0.0
Pillow>=10.0.0

# --- LangChain stack ---
langchain==0.3.7
langchain-experimental==0.3.3
langchain-community==0.3.7
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.2

# --- Embeddings ---
sentence-transformers==3.2.1

# --- Vector store ---
qdrant-client==1.12.1

# --- HTTP / config ---
requests==2.32.3
python-dotenv==1.0.1

# --- Numerics ---
numpy==1.26.4

# --- Frontend (app.py) ---
streamlit>=1.36.0
